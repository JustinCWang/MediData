# MediData Backend Environment Variables
# 
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control (it's in .gitignore)
#
# To use this file:
#   1. Copy .env.example to .env: cp .env.example .env (or copy .env.example .env on Windows)
#   2. Replace the placeholder values below with your actual Supabase credentials
#   3. Get your Supabase credentials from: https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Supabase Configuration
# Your Supabase project URL (found in Project Settings > API > Project URL)
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon/Public Key (found in Project Settings > API > Project API keys > anon/public)
# This key is safe to use in client-side code, but should still be kept private
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Supabase Service Role Key (for admin operations)
# WARNING: Never expose this key in client-side code - use only in backend
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Gemini API Key (For Chatbot)
GEMINI_API_KEY=you-api-key-here

# Frontend origin for CORS and reset-password links (e.g., your Vercel frontend URL)
FRONTEND_URL=https://medidata-frontend.vercel.app
# Optional: override the password reset redirect URL (falls back to FRONTEND_URL/reset-password)
FRONTEND_RESET_PASSWORD_URL=
